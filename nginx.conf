# Static-Hosting der App + Reverse-Proxy zum mitgelieferten PeerServer.
# /peer wird an den peer-Container weitergereicht (WebSocket-Upgrade nötig),
# damit die Signalisierung über dieselbe Domain läuft wie die App.
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /peer/ {
        proxy_pass http://peer:9000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
